MODULE hello;
VAR
    a, b, c, result, n:INTEGER;

BEGIN
    n := (2 + 2) * 2;
    n := 2 + 2 * 2;

    a := 10;
    b := 3;
    c := 5;

    result := (a + b) * c;
    result := a - b DIV c;
    result := (a * b) MOD (c + 1);
    result := 80 DIV (2 * (10 - 6));

    RETURN result;
END hello.